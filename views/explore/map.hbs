<section class="mb-80" Header>
    <div class="container">
        <div class="row">
            <div class="col ">

                <h1 class="MapHeader mr-auto p-2">Explore<span id="prueba">.</span></h1>
                <h3 class='MapSubtitles mr-auto p-2'>Focus, Network & Chill</h3>
            </div>
            <div class="col">
                <p class="MapPar items-align center">How challenging can it be to identify the greatest locations to
                    work from?
                    Not at
                    all difficult! But
                    finding a location that suits the demands of digital nomads may be quite challenging! We are thus
                    based on actual experiences together.</p>
            </div>
        </div>
    </div>
</section>


<section Filters></section>
<div class="container pt-90 justify-content-center text-align-center">
    <h4 class="text-center fs-5 fw-bold mt-5">Check-Out places around the world</h2>
</div>

<div class="container pt-80">
    <div class="row mt-80">
        <div class="d-flex justify-content-center">
            <a href="?type=focus" class="btn btn-lg btn-info">Focus</a>
            <a href="?type=networking" class="btn btn-info">Network</a>
            <a href="?type=chill" class=" btn btn-info">Chill</a>
        </div>
    </div>
</div>


<figure id="exploreMap"></figure>

<div class="container pt-90 justify-content-center text-align-center">
    <h4 class="text-center fs-5 fw-bold mt-5">Want to share a place with the DNW community?</h2>
        <a href="/places/create" class="  justify-content-center btn btn-info ">Add it Here!</a>
</div>


<div class="container">
    <h1 class="placesHeader text-right fs-12 fw-bold mt-5 mb-5"> Top Places.<span id="prueba">.</span>. </h1>
</div>

<div id="container" class="cols">
    {{#each places}}
    <div class="box mt-3 mb-5" style="background-image: url({{placeImg}})">
        <div class="container mt-3">
            <h3 class='title'> <span id="prueba2">{{name}} </span></h3>
            <h4>{{type}}</h4>
            <h4> Country, city {{country}}, {{city}} </h4>
        </div>
        <div class="mt-5">

            {{#unless isFav}}
            <form action="/user/{{../user}}/fav-places/{{_id}}" method="POST">
                <input type="submit" value="♡" class="love btn btn-info">
            </form>
            {{/unless}}
            {{#if isFav}}
            <form action="/user/{{../user}}/fav-places/{{_id}}" method="POST">
                <input type="submit" value="♥" class="btn btn-info">
            </form>
            {{/if}}
            <div>
                {{!-- <a href="/places/edit/{{_id}}" class="btn btn-dark">Edit Place</a> --}}
                <a class="btn btn-inf" href=" /places/details/{{_id}}">Details</a>
            </div>
        </div>
    </div>

    {{/each}}
</div>
</div>

</div>

</div>
</div>

<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script src="/js/exploreMap.js"></script>
<script
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAJjUzeG0WQ_oEe9YaRoUNWI-lhWdPB4oE&libraries=places&callback=initMap"></script>